import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        customTeal: "#38827E",
      },
    },
  },
  plugins: [],

};
export default config;